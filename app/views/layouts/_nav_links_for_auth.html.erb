<ul class="nav navbar-nav navbar-right">
  <% if user_signed_in? %>
    <li><%= link_to 'Edit account', edit_user_registration_path, class: 'navbar-brand pages' %></li>
    <li><%= link_to 'Log out', destroy_user_session_path, :method=>'delete', class: 'navbar-brand pages' %></li>
    <% if current_user.try(:admin) %>
      <li><%= link_to 'Users', users_path, class: 'navbar-brand pages' %></li>
    <% end %>
  <% else %>
    <li> <button type="button" style="margin-top: 10px" class="btn btn-primary" data-toggle="modal" data-target="#login" aria-label="Left Align" >Login</button></li>
    
  <% end %>

  <% if user_signed_in? %>
    
  <% end %>
</ul>

