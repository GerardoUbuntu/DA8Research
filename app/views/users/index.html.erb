<%= react_component 'Users', { data: @users, pages: @pages, page: @page} %>
<!---
<div class="container">
  <div class="row">
    <h3>Users</h3>
    <div class="column">
      <table class="table table-striped">
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <%= render user %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>  -->